var nombreUsuario = Fabian;
var tipoUsuario = colaborador;
var operacion = Creacion;

switch (nombreUsuario) {
    case (tipoUsuario == 'admin'):
            console.log('')
        break;

    default:
        break;
}