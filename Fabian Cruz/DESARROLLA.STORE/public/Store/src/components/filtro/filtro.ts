import { Component } from '@angular/core';

@Component({
  selector: 'filtro-menu', // Asignar un nombre de etiqueta, únio
  templateUrl: './filtro.html', // Asignar la ruta del archivo .html que represente esta vista
  styleUrls: ['./filtro.css'] // Un arreglo con las rutas de los CSS que queremos en este componente
})

// Debemos asignarle el nombre de nuestro componente
// 
//  Cambiar el nombre de AppComponent por el del nuestro
export class FiltroComponent { 
  
}
